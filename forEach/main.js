/* Ciclo forEach 
- array
- forEach()
- arrow function () => {}
- (element, index, array)

const names = ['marria', 'roma', 'francesca'];

names.forEach((element, i, array) => {
    console.log(element, i);
    console.log(array);
})
*/

